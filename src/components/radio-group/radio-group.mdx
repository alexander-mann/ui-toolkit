import { Canvas, Controls, Meta, Source, Title } from '@storybook/blocks'

import { RadioGroup } from '..'

import * as Stories from './radio-group.stories'

<Meta of={Stories} />

<Title />

#### A grouped set of radio inputs sharing a name, with support for labels and error states.

<Source
  dark
  language="tsx"
  code={`
import { RadioGroup } from '@alexandermann/ui-toolkit'

<RadioGroup
  label="Preferred contact method"
  name="contact"
  options={[
    { label: 'Email', value: 'email' },
    { label: 'Phone', value: 'phone' },
    { label: 'Text message', value: 'sms' },
  ]}
/>
`} />

## Properties

<Controls />

<Canvas of={Stories.BasicUsage} sourceState="none" />
