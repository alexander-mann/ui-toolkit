
import { Canvas, Controls, Meta, Source, Title } from '@storybook/blocks'

import { Button, Modal } from '..'

import * as Stories from './modal.stories'

<Meta of={Stories} />

<Title />

#### A flexible modal dialog component that provides a focused overlay interface for content, forms, or interactive elements. The modal can be triggered by any custom element and includes built-in accessibility features.

<Source
  dark
  language="tsx"
  code={`
import { Modal } from '@interval-network/ui-toolkit'

<Modal
  {...props}
  triggerElement={
    <span className="underline hover:opacity-80">Open Modal</span>
  }
  title="Title"
>
  Content goes here...
</Modal>
`} />

## Properties

<Controls />

<Canvas of={Stories.BasicUsage} sourceState="none" />
