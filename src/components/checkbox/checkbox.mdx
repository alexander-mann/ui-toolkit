import { Canvas, Controls, Meta, Source, Title } from '@storybook/blocks'

import { Checkbox } from '..'

import * as Stories from './checkbox.stories'

<Meta of={Stories} />

<Title />

#### A checkbox input with support for labels, required indicators, and error states.

<Source
  dark
  language="tsx"
  code={`
import { Checkbox } from '@alexandermann/ui-toolkit'

<Checkbox
  id="terms"
  label="I agree to the terms and conditions"
  hasError={false}
  errorMessage="You must accept the terms to continue."
/>
`} />

## Properties

<Controls />

<Canvas of={Stories.BasicUsage} sourceState="none" />
