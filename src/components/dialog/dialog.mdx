
import { Canvas, Controls, Meta, Source, Title } from '@storybook/blocks'

import { Button, Dialog } from '..'

import * as Stories from './dialog.stories'

<Meta of={Stories} />

<Title />

#### A flexible dialog component that provides a focused overlay interface for content, forms, or interactive elements. The modal can be triggered by any custom element and includes built-in accessibility features.

<Source
  dark
  language="tsx"
  code={`
import { Dialog } from '@interval-network/ui-toolkit'

<Dialog
  triggerElement={
    <span className="underline hover:opacity-80">Open Modal</span>
  }
  title="Title"
>
  Content goes here...
</Dialog>
`} />

## Properties

<Controls />

<Canvas of={Stories.BasicUsage} sourceState="none" />
