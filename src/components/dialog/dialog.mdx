
import { Canvas, Controls, Meta, Source, Title } from '@storybook/blocks'

import { Button, Dialog } from '..'

import * as Stories from './dialog.stories'

<Meta of={Stories} />

<Title />

#### A flexible dialog component that provides a focused overlay interface for content, forms, or interactive elements. The modal can be triggered by any custom element and includes built-in accessibility features.

<Source
  dark
  language="tsx"
  code={`
import { Dialog } from '@alexandermann/ui-toolkit'

<Dialog
  triggerElement={
    <a className="underline hover:opacity-80">Open Modal</a>
  }
  title="Title"
>
  Content goes here...
</Dialog>
`} />

## Properties

<Controls />

<Canvas of={Stories.BasicUsage} sourceState="none" />

### Variants

#### Action Dialog
<Canvas of={Stories.ActionDialog} sourceState="none" />

### Accessibility

The dialog is labelled by its `title`, which renders as an `h2` — a dialog heading is a section heading, not the page title, so an `h1` would compete with the host page's own. Set `headingLevel` if a different level suits the surrounding document outline.
